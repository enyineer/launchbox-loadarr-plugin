<Window x:Class="Loadarr.UI.BigBoxSearchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="clr-namespace:Loadarr.UI"
        x:ClassModifier="internal"
        Title="Loadarr — BigBox"
        WindowStyle="None"
        WindowState="Maximized"
        ResizeMode="NoResize"
        Topmost="True"
        AllowsTransparency="False"
        ShowInTaskbar="False"
        FontFamily="Segoe UI"
        Foreground="#E6E8EE"
        Background="#0D0F14">

    <Window.DataContext>
        <ui:SearchWindowViewModel />
    </Window.DataContext>

    <Window.Resources>
        <SolidColorBrush x:Key="Surface"       Color="#1A1D26"/>
        <SolidColorBrush x:Key="SurfaceAlt"    Color="#252A38"/>
        <SolidColorBrush x:Key="Border"        Color="#3A3F4F"/>
        <SolidColorBrush x:Key="TextPrimary"   Color="#E6E8EE"/>
        <SolidColorBrush x:Key="TextDim"       Color="#9097A8"/>
        <SolidColorBrush x:Key="Accent"        Color="#F0A958"/>
        <SolidColorBrush x:Key="AccentHover"   Color="#F5BD7A"/>
        <SolidColorBrush x:Key="AccentPressed" Color="#D89043"/>
        <SolidColorBrush x:Key="FocusRing"     Color="#FFFFFFFF"/>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
        </Style>

        <!-- Virtual keyboard key. Focusable; Enter / A button activates Click. -->
        <Style x:Key="KeyButton" TargetType="Button">
            <Setter Property="Background" Value="#22FFFFFF"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Border}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="22"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="MinWidth" Value="58"/>
            <Setter Property="MinHeight" Value="58"/>
            <Setter Property="Margin" Value="3"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource Accent}"/>
                                <Setter TargetName="bd" Property="BorderThickness" Value="3"/>
                                <Setter TargetName="bd" Property="Background" Value="#44F0A958"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#88F0A958"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Wider variant for ␣ / ⌫ / ✕ / ⌄ / SEARCH. Larger font so the
             unicode icons are unambiguous at couch distance. -->
        <Style x:Key="WideKeyButton" TargetType="Button" BasedOn="{StaticResource KeyButton}">
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="FontFamily" Value="Segoe UI Symbol, Segoe UI"/>
        </Style>

        <Style x:Key="AccentKeyButton" TargetType="Button" BasedOn="{StaticResource KeyButton}">
            <Setter Property="Background" Value="{StaticResource Accent}"/>
            <Setter Property="Foreground" Value="#1A1300"/>
            <Setter Property="MinWidth" Value="160"/>
            <Setter Property="FontSize" Value="20"/>
        </Style>

        <Style x:Key="ResultItem" TargetType="ListBoxItem">
            <Setter Property="Background" Value="#33252A38"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Padding" Value="18,14"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#552C3346"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource Accent}"/>
                                <Setter TargetName="bd" Property="Background" Value="#66F0A958"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="QueueItem" TargetType="ListBoxItem">
            <Setter Property="Background" Value="#33252A38"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Padding" Value="14,10"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ListBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        </Style>

        <!-- Dark scrollbar that matches the BigBox theme. Same shape as the
             desktop SearchWindow scrollbar but a touch wider (14px vs 10px) so
             it reads at couch distance. -->
        <Style x:Key="ScrollThumb" TargetType="Thumb">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="IsTabStop" Value="False"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border x:Name="bd"
                                Background="#553A3F4F"
                                CornerRadius="4"
                                Margin="3"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#88F0A958"/>
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{StaticResource Accent}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ScrollPageButton" TargetType="RepeatButton">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="IsTabStop" Value="False"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border Background="Transparent"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <ControlTemplate x:Key="VerticalScrollBarTemplate" TargetType="ScrollBar">
            <Grid Background="#221B1E27">
                <Track x:Name="PART_Track" IsDirectionReversed="True">
                    <Track.DecreaseRepeatButton>
                        <RepeatButton Style="{StaticResource ScrollPageButton}"
                                      Command="ScrollBar.PageUpCommand"/>
                    </Track.DecreaseRepeatButton>
                    <Track.Thumb>
                        <Thumb Style="{StaticResource ScrollThumb}"/>
                    </Track.Thumb>
                    <Track.IncreaseRepeatButton>
                        <RepeatButton Style="{StaticResource ScrollPageButton}"
                                      Command="ScrollBar.PageDownCommand"/>
                    </Track.IncreaseRepeatButton>
                </Track>
            </Grid>
        </ControlTemplate>

        <ControlTemplate x:Key="HorizontalScrollBarTemplate" TargetType="ScrollBar">
            <Grid Background="#221B1E27">
                <Track x:Name="PART_Track" IsDirectionReversed="False">
                    <Track.DecreaseRepeatButton>
                        <RepeatButton Style="{StaticResource ScrollPageButton}"
                                      Command="ScrollBar.PageLeftCommand"/>
                    </Track.DecreaseRepeatButton>
                    <Track.Thumb>
                        <Thumb Style="{StaticResource ScrollThumb}"/>
                    </Track.Thumb>
                    <Track.IncreaseRepeatButton>
                        <RepeatButton Style="{StaticResource ScrollPageButton}"
                                      Command="ScrollBar.PageRightCommand"/>
                    </Track.IncreaseRepeatButton>
                </Track>
            </Grid>
        </ControlTemplate>

        <Style TargetType="ScrollBar">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Vertical">
                    <Setter Property="Width" Value="14"/>
                    <Setter Property="Height" Value="Auto"/>
                    <Setter Property="Template" Value="{StaticResource VerticalScrollBarTemplate}"/>
                </Trigger>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Width" Value="Auto"/>
                    <Setter Property="Height" Value="14"/>
                    <Setter Property="Template" Value="{StaticResource HorizontalScrollBarTemplate}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- ScrollViewer template fills the bottom-right corner. -->
        <Style TargetType="ScrollViewer">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollViewer">
                        <Grid Background="{TemplateBinding Background}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <ScrollContentPresenter Grid.Column="0" Grid.Row="0"
                                                    CanContentScroll="{TemplateBinding CanContentScroll}"
                                                    Margin="{TemplateBinding Padding}"/>

                            <ScrollBar x:Name="PART_VerticalScrollBar"
                                       Grid.Column="1" Grid.Row="0"
                                       Orientation="Vertical"
                                       Maximum="{TemplateBinding ScrollableHeight}"
                                       ViewportSize="{TemplateBinding ViewportHeight}"
                                       Value="{Binding VerticalOffset, RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay}"
                                       Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"/>

                            <ScrollBar x:Name="PART_HorizontalScrollBar"
                                       Grid.Column="0" Grid.Row="1"
                                       Orientation="Horizontal"
                                       Maximum="{TemplateBinding ScrollableWidth}"
                                       ViewportSize="{TemplateBinding ViewportWidth}"
                                       Value="{Binding HorizontalOffset, RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay}"
                                       Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"/>

                            <Rectangle Grid.Column="1" Grid.Row="1" Fill="#1B1E27"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ProgressBar">
            <Setter Property="Background" Value="#33000000"/>
            <Setter Property="Foreground" Value="{StaticResource Accent}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Border}"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.Background>
            <SolidColorBrush Color="#0D0F14"/>
        </Grid.Background>

        <Image Source="pack://application:,,,/Loadarr;component/UI/Assets/bg.jpg"
               Stretch="UniformToFill"
               Opacity="0.30"
               IsHitTestVisible="False"/>

        <Grid IsHitTestVisible="False">
            <Grid.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#CC0D0F14" Offset="0"/>
                    <GradientStop Color="#770D0F14" Offset="0.45"/>
                    <GradientStop Color="#CC0D0F14" Offset="1"/>
                </LinearGradientBrush>
            </Grid.Background>
        </Grid>

        <Grid Margin="64,40,64,32">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
                <TextBlock Text="L O A D A R R"
                           FontSize="44" FontWeight="Bold"
                           Foreground="{StaticResource Accent}"/>
                <TextBlock Text="R O M   S E A R C H"
                           FontSize="20" FontWeight="SemiBold"
                           Foreground="{StaticResource TextDim}"
                           VerticalAlignment="Bottom"
                           Margin="22,0,0,8"/>
            </StackPanel>

            <!-- Query field: focusable Button styled as a text input. Pressing
                 A on it shows the keyboard. The caret blinks so the user has
                 an unambiguous "you're typing here" cue when keyboard is up. -->
            <Button x:Name="QueryField"
                    Grid.Row="1"
                    Margin="0,0,0,18"
                    Click="OnQueryFieldClick"
                    HorizontalContentAlignment="Stretch"
                    FocusVisualStyle="{x:Null}">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd"
                                Background="#CC1B1E27"
                                BorderBrush="{StaticResource Border}"
                                BorderThickness="2"
                                CornerRadius="4"
                                Padding="20,14">
                            <ContentPresenter HorizontalAlignment="Stretch"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource Accent}"/>
                                <Setter TargetName="bd" Property="BorderThickness" Value="3"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
                <Grid>
                    <!-- Hint shown when Query is empty. -->
                    <TextBlock Text="Type to search…"
                               FontSize="32"
                               Foreground="{StaticResource TextDim}"
                               VerticalAlignment="Center"
                               IsHitTestVisible="False">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Query}" Value="">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Query}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="{Binding Query}"
                                   FontSize="32"
                                   Foreground="{StaticResource TextPrimary}"/>
                        <Border x:Name="QueryCaret"
                                Width="3"
                                Height="36"
                                Background="{StaticResource Accent}"
                                Margin="3,0,0,0"
                                VerticalAlignment="Center">
                            <Border.Triggers>
                                <EventTrigger RoutedEvent="Border.Loaded">
                                    <BeginStoryboard>
                                        <Storyboard RepeatBehavior="Forever">
                                            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="Opacity">
                                                <DiscreteDoubleKeyFrame KeyTime="0:0:0"   Value="1"/>
                                                <DiscreteDoubleKeyFrame KeyTime="0:0:0.5" Value="0"/>
                                                <DiscreteDoubleKeyFrame KeyTime="0:0:1"   Value="1"/>
                                            </DoubleAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Border.Triggers>
                        </Border>
                    </StackPanel>
                </Grid>
            </Button>

            <!-- Two-column body: results | queue -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="22"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Results -->
                <Border Grid.Column="0"
                        Background="#22000000"
                        BorderBrush="{StaticResource Border}"
                        BorderThickness="1"
                        CornerRadius="4">
                    <Grid Margin="14">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0"
                                   Text="RESULTS"
                                   FontSize="14" FontWeight="Bold"
                                   Foreground="{StaticResource Accent}"
                                   Margin="4,0,0,10"/>
                        <ListBox x:Name="ResultsList"
                                 Grid.Row="1"
                                 ItemsSource="{Binding Results}"
                                 SelectedItem="{Binding SelectedResult}"
                                 ItemContainerStyle="{StaticResource ResultItem}"
                                 KeyboardNavigation.DirectionalNavigation="Continue"
                                 Focusable="False"
                                 MouseDoubleClick="OnResultDoubleClick">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="{Binding Title}"
                                                       FontSize="22" FontWeight="SemiBold"
                                                       TextTrimming="CharacterEllipsis"/>
                                            <TextBlock Margin="0,4,0,0"
                                                       FontSize="14"
                                                       Foreground="{StaticResource TextDim}">
                                                <Run Text="{Binding Platform, Mode=OneWay}"/>
                                                <Run Text="  •  "/>
                                                <Run Text="{Binding SourceName, Mode=OneWay}"/>
                                                <Run Text="  •  "/>
                                                <Run Text="{Binding Region, Mode=OneWay}"/>
                                            </TextBlock>
                                        </StackPanel>
                                        <TextBlock Grid.Column="1"
                                                   Text="{Binding DisplaySize}"
                                                   FontSize="16"
                                                   Foreground="{StaticResource TextDim}"
                                                   VerticalAlignment="Center"
                                                   Margin="20,0,0,0"/>
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Border>

                <!-- Queue panel (inline, always visible) -->
                <Border Grid.Column="2"
                        Background="#22000000"
                        BorderBrush="{StaticResource Border}"
                        BorderThickness="1"
                        CornerRadius="4">
                    <Grid Margin="14">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0"
                                   Text="QUEUE"
                                   FontSize="14" FontWeight="Bold"
                                   Foreground="{StaticResource Accent}"
                                   Margin="4,0,0,10"/>
                        <Grid Grid.Row="1">
                            <TextBlock x:Name="QueueEmptyHint"
                                       Text="No downloads yet."
                                       FontSize="14"
                                       Foreground="{StaticResource TextDim}"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       Visibility="Collapsed"/>
                            <ListBox x:Name="QueueList"
                                     ItemContainerStyle="{StaticResource QueueItem}"
                                     Focusable="False">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel>
                                            <TextBlock Text="{Binding Title}"
                                                       FontSize="16" FontWeight="SemiBold"
                                                       TextTrimming="CharacterEllipsis"/>
                                            <Grid Margin="0,6,0,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <ProgressBar Grid.Column="0"
                                                             Height="6"
                                                             Minimum="0" Maximum="100"
                                                             Value="{Binding ProgressValue}"
                                                             IsIndeterminate="{Binding ProgressIndeterminate}"
                                                             VerticalAlignment="Center"/>
                                                <TextBlock Grid.Column="1"
                                                           Text="{Binding StatusText}"
                                                           Margin="10,0,0,0"
                                                           FontSize="12"
                                                           Foreground="{StaticResource TextDim}"
                                                           VerticalAlignment="Center"/>
                                            </Grid>
                                            <TextBlock Text="{Binding StatusDetail}"
                                                       Margin="0,4,0,0"
                                                       FontSize="11"
                                                       Foreground="{StaticResource TextDim}"
                                                       TextTrimming="CharacterEllipsis"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Grid>
                    </Grid>
                </Border>
            </Grid>

            <!-- Status row -->
            <Grid Grid.Row="3" Margin="0,12,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <ProgressBar Grid.Column="0" Height="3"
                             Minimum="0" Maximum="100"
                             Value="{Binding ProgressValue}"
                             IsIndeterminate="{Binding ProgressIndeterminate}"
                             VerticalAlignment="Center"/>
                <TextBlock Grid.Column="1"
                           Text="{Binding StatusText}"
                           Margin="14,0,0,0"
                           FontSize="14"
                           Foreground="{StaticResource TextDim}"
                           VerticalAlignment="Center"/>
            </Grid>

            <!-- Virtual keyboard + hints -->
            <Grid Grid.Row="4" Margin="0,16,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Keyboard: two uniform grids stacked. Filled in code-behind.
                     Initial state is collapsed so the queue is immediately
                     visible; user presses X (or A on the query field) to show
                     it, types, then SEARCH (or hides via the HIDE key). -->
                <StackPanel x:Name="KeyboardPanel"
                            Grid.Row="0"
                            Visibility="Collapsed"
                            KeyboardNavigation.DirectionalNavigation="Continue">
                    <UniformGrid x:Name="KeyboardKeys"
                                 Rows="4"
                                 Columns="10"
                                 HorizontalAlignment="Stretch"/>
                    <UniformGrid x:Name="KeyboardSpecial"
                                 Rows="1"
                                 Columns="5"
                                 Margin="0,6,0,0"
                                 HorizontalAlignment="Stretch"/>
                </StackPanel>

                <!-- Controller hint bar. -->
                <StackPanel Grid.Row="1"
                            Orientation="Horizontal"
                            HorizontalAlignment="Center"
                            Margin="0,14,0,0">
                    <TextBlock Text="A: Confirm"     FontSize="13" Foreground="{StaticResource TextDim}" Margin="0,0,28,0"/>
                    <TextBlock Text="B: Back"        FontSize="13" Foreground="{StaticResource TextDim}" Margin="0,0,28,0"/>
                    <TextBlock Text="X: Keyboard"    FontSize="13" Foreground="{StaticResource TextDim}" Margin="0,0,28,0"/>
                    <TextBlock Text="Start: Close"   FontSize="13" Foreground="{StaticResource TextDim}"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</Window>
