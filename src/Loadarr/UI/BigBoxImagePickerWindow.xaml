<Window x:Class="Loadarr.UI.BigBoxImagePickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:ClassModifier="internal"
        Title="Loadarr — Select Images"
        WindowStyle="None"
        WindowState="Maximized"
        ResizeMode="NoResize"
        Topmost="True"
        ShowInTaskbar="False"
        FontFamily="Segoe UI"
        Foreground="#E6E8EE"
        Background="#0D0F14">

    <Window.Resources>
        <SolidColorBrush x:Key="Surface"     Color="#1A1D26"/>
        <SolidColorBrush x:Key="Border"      Color="#3A3F4F"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#E6E8EE"/>
        <SolidColorBrush x:Key="TextDim"     Color="#9097A8"/>
        <SolidColorBrush x:Key="Accent"      Color="#F0A958"/>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
        </Style>

        <Style x:Key="OptionItem" TargetType="ListBoxItem">
            <Setter Property="Background" Value="#33252A38"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Padding" Value="22,14"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource Accent}"/>
                                <Setter TargetName="bd" Property="Background" Value="#66F0A958"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ListBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        </Style>

        <!-- Dark scrollbar matching BigBoxSearchWindow. -->
        <Style x:Key="ScrollThumb" TargetType="Thumb">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="IsTabStop" Value="False"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border x:Name="bd"
                                Background="#553A3F4F"
                                CornerRadius="4"
                                Margin="3"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#88F0A958"/>
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{StaticResource Accent}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ScrollPageButton" TargetType="RepeatButton">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="IsTabStop" Value="False"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border Background="Transparent"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <ControlTemplate x:Key="VerticalScrollBarTemplate" TargetType="ScrollBar">
            <Grid Background="#221B1E27">
                <Track x:Name="PART_Track" IsDirectionReversed="True">
                    <Track.DecreaseRepeatButton>
                        <RepeatButton Style="{StaticResource ScrollPageButton}"
                                      Command="ScrollBar.PageUpCommand"/>
                    </Track.DecreaseRepeatButton>
                    <Track.Thumb>
                        <Thumb Style="{StaticResource ScrollThumb}"/>
                    </Track.Thumb>
                    <Track.IncreaseRepeatButton>
                        <RepeatButton Style="{StaticResource ScrollPageButton}"
                                      Command="ScrollBar.PageDownCommand"/>
                    </Track.IncreaseRepeatButton>
                </Track>
            </Grid>
        </ControlTemplate>

        <Style TargetType="ScrollBar">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Vertical">
                    <Setter Property="Width" Value="14"/>
                    <Setter Property="Template" Value="{StaticResource VerticalScrollBarTemplate}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="ScrollViewer">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollViewer">
                        <Grid Background="{TemplateBinding Background}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ScrollContentPresenter Grid.Column="0"
                                                    CanContentScroll="{TemplateBinding CanContentScroll}"
                                                    Margin="{TemplateBinding Padding}"/>
                            <ScrollBar x:Name="PART_VerticalScrollBar"
                                       Grid.Column="1"
                                       Orientation="Vertical"
                                       Maximum="{TemplateBinding ScrollableHeight}"
                                       ViewportSize="{TemplateBinding ViewportHeight}"
                                       Value="{Binding VerticalOffset, RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay}"
                                       Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.Background>
            <SolidColorBrush Color="#0D0F14"/>
        </Grid.Background>

        <Image Source="pack://application:,,,/Loadarr;component/UI/Assets/bg.jpg"
               Stretch="UniformToFill"
               Opacity="0.30"
               IsHitTestVisible="False"/>

        <Grid IsHitTestVisible="False">
            <Grid.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#CC0D0F14" Offset="0"/>
                    <GradientStop Color="#770D0F14" Offset="0.45"/>
                    <GradientStop Color="#CC0D0F14" Offset="1"/>
                </LinearGradientBrush>
            </Grid.Background>
        </Grid>

        <Grid Margin="120,60,120,40" MaxWidth="1400" HorizontalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <TextBlock x:Name="HeaderText"
                       Grid.Row="0"
                       FontSize="36" FontWeight="Bold"
                       Foreground="{StaticResource Accent}"
                       Margin="0,0,0,12"/>

            <!-- Subtitle -->
            <TextBlock x:Name="SubtitleText"
                       Grid.Row="1"
                       FontSize="18"
                       Foreground="{StaticResource TextDim}"
                       Margin="0,0,0,24"
                       TextWrapping="Wrap"/>

            <!-- Options list -->
            <Border Grid.Row="2"
                    Background="#22000000"
                    BorderBrush="{StaticResource Border}"
                    BorderThickness="1"
                    CornerRadius="4">
                <ListBox x:Name="OptionsList"
                         Margin="14"
                         ItemContainerStyle="{StaticResource OptionItem}"
                         KeyboardNavigation.DirectionalNavigation="Contained">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="44"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <!-- Visual checkbox: filled square when selected, hollow otherwise. -->
                                <Border Grid.Column="0"
                                        Width="28" Height="28"
                                        BorderBrush="{StaticResource Accent}"
                                        BorderThickness="2"
                                        CornerRadius="3"
                                        VerticalAlignment="Center">
                                    <Border Background="{StaticResource Accent}"
                                            Margin="3"
                                            CornerRadius="1">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                    </Border>
                                </Border>

                                <TextBlock Grid.Column="1"
                                           Text="{Binding TypeText}"
                                           FontSize="22" FontWeight="SemiBold"
                                           VerticalAlignment="Center"
                                           Margin="14,0,0,0"/>

                                <TextBlock Grid.Column="2"
                                           Text="{Binding RegionText}"
                                           FontSize="18"
                                           Foreground="{StaticResource TextDim}"
                                           VerticalAlignment="Center"/>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>

            <!-- Hint bar -->
            <StackPanel Grid.Row="3"
                        Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        Margin="0,18,0,0">
                <TextBlock Text="A: Toggle"          FontSize="13" Foreground="{StaticResource TextDim}" Margin="0,0,28,0"/>
                <TextBlock Text="X: Deselect All"    FontSize="13" Foreground="{StaticResource TextDim}" Margin="0,0,28,0"/>
                <TextBlock Text="Y: Select All"      FontSize="13" Foreground="{StaticResource TextDim}" Margin="0,0,28,0"/>
                <TextBlock Text="Start: Confirm"     FontSize="13" Foreground="{StaticResource TextDim}" Margin="0,0,28,0"/>
                <TextBlock Text="B: Cancel"          FontSize="13" Foreground="{StaticResource TextDim}"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
